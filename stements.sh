#!/bin/bash
echo -e "$#  :  total number of arguments passed \n$* : all arguments passd as string \n$@  :all arguments passed as array \n$0 : name of the script \n $$  : processid of the current running process \n$! : processs id of the of the last command went in the background \n$?  : used to print"
